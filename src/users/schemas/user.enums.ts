export enum UserRole {
  CLIENT = 'client',
  VENDOR = 'vendor',
  ADMIN = 'admin',
}

export enum UserStatus {
  ACTIVE = 'active',
  BLOCKED = 'blocked',
  PENDING = 'pending',
}

export enum RegistrationType {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
  APPLE = 'apple',
  EMAIL = 'email',
  PHONE = 'phone',
}
